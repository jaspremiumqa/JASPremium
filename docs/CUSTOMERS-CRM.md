# Customers CRM

The Customers section reads from Supabase `customers` and provides:
- search by name, phone, or email
- add/edit customer
- customer notes
- booking history through `bookings` and `booking_services`
- booking count and total spent

No JSON customer catalogue is used.
